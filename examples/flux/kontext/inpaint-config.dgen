#! /usr/bin/env dgenerate --file
#! dgenerate 5.0.0

\set token %HF_TOKEN%

{% if not token.strip() and not '--auth-token' in injected_args %}
    \print Set HF_TOKEN environmental variable or --auth-token to run this example!
    \exit
{% endif %}

{% if have_cuda() and have_feature('bitsandbytes') and total_memory(unit='gib') > 15 %}
    \set optimization --quantizer bnb;bits=4
{% else %}
    \set optimization --model-sequential-offload
{% endif %}


black-forest-labs/FLUX.1-Kontext-dev
--model-type flux-kontext {{ optimization }}
--image-seeds "../../media/dog-on-bench.png;../../media/dog-on-bench-mask.png"
--dtype bfloat16
--inference-steps 50
--guidance-scales 3.5
--gen-seeds 1
--output-path inpaint
--output-size 1024
--prompts "A fluffy orange cat sitting on a park bench"